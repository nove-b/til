``` bash
code -r xxx.ts
```

Aフォルダ内のファイルを修正しているときに、Bフォルダにあるファイルを参照したいことがある。

今までは、わざわざBフォルダをVS Codeで開いてファイルを確認していた。

`code -r` を使えば、Aフォルダを開いているVS Codeのウィンドウをそのまま使って、Bフォルダのファイルを開ける。

``` bash
code -r ../B/xxx.ts
```

ちょっとした別プロジェクトのコード確認に便利。
